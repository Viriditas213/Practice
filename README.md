介绍：
这是一个包含打开和关闭动画的背包系统，所有的面板和背包格子都会在关闭时放入缓存池中，以减少重复创建带来的性能和内存的消耗;
为了消除初次打开背包面板时创建背包面板带来的动画上的卡顿，所有的面板都在进入游戏时异步完成了预加载;
同样的，背包格子也应用了后台自动加载功能，当玩家每获得一个物品时，背包在后台就会自动进行检查，当所空余的背包格子小于10个时，背包就会在后台自动异步创建格子;
B键打开主面板，N键关闭主面板，目前只做了背包系统，后续会逐步完善对话系统和任务系统;
